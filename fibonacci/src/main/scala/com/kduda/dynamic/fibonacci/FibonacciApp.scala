package com.kduda.dynamic.fibonacci

object FibonacciApp {
  def main(args: Array[String]): Unit = {
//    for { x <- 0 to 1000 } println(s"x = $x ${com.kduda.dynamic.fibonacci.FibonacciNaive(x)}") // takes to long when when n > 30
//    for { x <- 0 to 4000 } println(s"x = $x ${com.kduda.dynamic.fibonacci.FibonacciNaiveMemoization(x)}") // stack overflow when n > 3000
//    for { x <- 0 to 10000 } println(s"x = $x ${com.kduda.dynamic.fibonacci.FibonacciTailrec(x)}") // no stack overflow, and works surprisingly fast
//    for { x <- 0 to 10000 } println(s"x = $x ${com.kduda.dynamic.fibonacci.FibonacciStream(x)}") // works great
//    for { x <- 0 to 10000 } println(s"x = $x ${com.kduda.dynamic.fibonacci.FibonacciBottomUp(x)}") // works great
  }
}
